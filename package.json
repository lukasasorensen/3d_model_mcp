{
  "name": "3d-model-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first conversational CAD using MCP tools and OpenSCAD.",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "dev": "pnpm --filter @rjls/site dev",
    "dev:site": "pnpm --filter @rjls/site dev",
    "build": "pnpm build:packages && pnpm --filter @rjls/site build",
    "build:packages": "pnpm --filter @rjls/contracts build && pnpm --filter @rjls/model-project build && pnpm --filter @rjls/renderer build && pnpm --filter @rjls/mcp build && pnpm --filter @rjls/gateway build && pnpm --filter @rjls/runtime build",
    "typecheck": "pnpm build:packages && pnpm --filter @rjls/site typecheck",
    "lint": "eslint . --ext .js,.mjs,.ts,.tsx --max-warnings=0",
    "test": "pnpm build:packages && pnpm --filter './packages/*' test && pnpm --filter @rjls/site test",
    "check:boundaries": "node scripts/check-package-boundaries.mjs",
    "test:boundaries": "node --test scripts/check-package-boundaries.test.mjs",
    "test:renderer:strict": "node scripts/check-render-environment.mjs",
    "check:leaks": "node scripts/check-template-leaks.mjs",
    "clean": "pnpm --filter './packages/*' clean && pnpm --filter @rjls/site clean",
    "verify": "pnpm check:leaks && pnpm check:boundaries && pnpm test:boundaries && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm check:leaks && pnpm check:boundaries"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "eslint": "^8.57.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
